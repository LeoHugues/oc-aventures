{% extends 'WebSiteBundle:Back:baseBackOffice.html.twig' %}
{% block title %}
    Administration
{% endblock %}
{% block content %}
    <div class="well">
        <h1>Administrer les images du site :</h1> <br>
        <p>Modifier les images du site. </p>
        <a class="btn-default btn" href="{{ path('admin_images') }}">Modifier les images</a>
    </div>

    <div class="well">

        <h1>Administrer les textes du site :</h1> <br>

        <div>
            <label>Modifier les textes en français. </label><br>
            <a class="btn-default btn" href="{{ path('admin_text', {'lang' : 'fr'}) }}">Modifier les textes</a>
        </div>
        <div>
            <label>Modifier les textes en anglais. </label><br>
            <a class="btn-default btn" href="{{ path('admin_text', {'lang' : 'en'}) }}">Modifier les textes</a>
        </div>
        <div>
            <label>Modifier les textes en allemend. </label><br>
            <a class="btn-default btn" href="{{ path('admin_text', {'lang' : 'de'}) }}">Modifier les textes</a>
        </div>
    </div>

    <div class="well">
        <h1>Date d'ouverture et de fermeture du site :</h1> <br>
        <p>Modifier les dates d'ouvertures du site : </p>
        <a class="btn-default btn" href="{{ path('admin_date_ouvertures') }}">Modifier les dates</a>
    </div>
{% endblock %}
