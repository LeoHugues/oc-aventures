{% extends 'WebSiteBundle:Front:base.html.twig' %}
    {% block title %}
        Ouvertures
    {% endblock %}
{% block content %}
    {% trans_default_domain "ouvertures" %}
    <div class="row">
    <div class="box">
        <div class="text-center text-yellow">
            <h1>{{ 'titre_ouvertures'|trans|raw }}</h1>
            <br>
        </div>
        <div class="col-lg-12">
            <p>
                {{ 'texte_ouvertures'|trans|raw }}
            </p>
            <hr>
            <h3 class="text-center text-yellow"> {{ 'titre_hors_saison'|trans|raw }}</h3>
            <p>
                {{ 'texte_hors_saison'|trans|raw }}
            </p>
            <hr>
            <h3 class="text-center text-yellow"> {{ 'titre_saison'|trans|raw }}</h3>
            <p>
                {{ 'texte_saison'|trans|raw }}
            </p>
            <hr>
            <p> {{ 'texte_supp'|trans|raw }}
            </p>
        </div>
    </div>
</div>
{% endblock %}